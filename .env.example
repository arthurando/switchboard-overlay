# Cloudflare R2 Configuration
# Copy this file to .env.local and fill in your actual values

# R2 Account ID (from Cloudflare Dashboard)
R2_ACCOUNT_ID=your_account_id_here

# R2 API Token Credentials
R2_ACCESS_KEY_ID=your_access_key_here
R2_SECRET_ACCESS_KEY=your_secret_access_key_here

# R2 Bucket Name
R2_BUCKET_NAME=switchboard-images

# R2 Public URL (choose ONE):
# Option 1: R2.dev subdomain (easiest, for free tier)
R2_DEV_SUBDOMAIN=pub-abc123.r2.dev

# Option 2: Custom domain (optional, more professional)
# R2_PUBLIC_DOMAIN=images.yourdomain.com

# Optional: Cron job secret for cleanup endpoint
# CRON_SECRET=random_secret_key_here

# ========================================
# How to get these values:
# ========================================
#
# 1. R2_ACCOUNT_ID:
#    - Go to Cloudflare Dashboard → R2
#    - Your account ID is shown in the URL or bucket settings
#
# 2. R2_ACCESS_KEY_ID & R2_SECRET_ACCESS_KEY:
#    - Cloudflare Dashboard → R2 → Manage R2 API Tokens
#    - Create API Token → Copy the credentials
#
# 3. R2_BUCKET_NAME:
#    - The name you gave your bucket (e.g., "switchboard-images")
#
# 4. R2_DEV_SUBDOMAIN:
#    - R2 → Your bucket → Settings → Public R2.dev Bucket URL
#    - Copy just the subdomain part (e.g., "pub-abc123.r2.dev")
#
# See R2_SETUP.md for detailed instructions!
